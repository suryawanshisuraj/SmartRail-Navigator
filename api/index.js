/**
 * Vercel Serverless Function entry point for SmartRail Navigator Backend API.
 * Routes all incoming /api/* requests directly through Express.
 */

import app from '../backend/src/app.js';

export default app;
